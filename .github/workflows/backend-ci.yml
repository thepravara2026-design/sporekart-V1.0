name: Java 21 Backend CI Pipeline

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'sporekart-backend/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'sporekart-backend/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build with Maven & Spring Modulith Tests
      run: |
        cd sporekart-backend
        mvn clean verify -B
